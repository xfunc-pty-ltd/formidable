namespace Formidable.WebApp.Fixture;

/// <summary>The quickstart's model, property for property: the smallest thing a hosting-model
/// test can drive that still has two fields, one of which fails two different ways.</summary>
public sealed class Contact
{
    public string Name { get; set; } = string.Empty;

    public string Email { get; set; } = string.Empty;
}
